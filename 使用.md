# 博客使用指南

## 🚀 启动项目

```bash
# 安装依赖
bun install

# 启动开发服务器
bun dev
```

## 📝 发布新文章

### 1. 创建文章文件

在 `content/blog/` 目录下创建 `.mdx` 文件，例如：`my-new-post.mdx`

### 2. 文章格式

```mdx
---
title: "文章标题"
date: "2024-11-26"
description: "文章简短描述"
tags: ["标签1", "标签2", "标签3"]
category: "分类名称"
author: "作者名"
coverImage: "/images/blog/my-cover.jpg"
---

# 正文标题

这里开始写你的文章内容...
```

## 🖼️ 封面图片使用

### 方式一：使用本地图片（推荐）

1. 将图片放到 `public/images/blog/` 目录下
2. 在文章中引用：

```yaml
coverImage: "/images/blog/your-image.jpg"
```

支持的图片格式：`.jpg`, `.jpeg`, `.png`, `.gif`, `.webp`

### 方式二：使用网络图片

直接使用完整的图片 URL：

```yaml
coverImage: "https://images.unsplash.com/photo-xxx?w=1200&h=600&fit=crop"
```

### 推荐图片尺寸

- **封面图**: 1200 x 600 像素（2:1 比例）
- **文件大小**: 建议 < 500KB（用于网页加载优化）

## 📁 目录结构

```
cursor-clone/
├── content/
│   └── blog/           # 📝 文章存放目录
│       ├── my-post.mdx
│       └── ...
├── public/
│   └── images/
│       └── blog/       # 🖼️ 博客图片目录
│           ├── cover1.jpg
│           └── ...
└── src/
    └── ...
```

## 🏷️ Frontmatter 字段说明

| 字段 | 必填 | 说明 |
|------|------|------|
| `title` | ✅ | 文章标题 |
| `date` | ✅ | 发布日期 (YYYY-MM-DD) |
| `description` | ✅ | 文章简介，显示在列表页 |
| `tags` | ✅ | 标签数组 |
| `category` | ✅ | 分类名称 |
| `author` | ❌ | 作者名（可选） |
| `coverImage` | ❌ | 封面图路径（可选，有默认图） |

## 💡 Markdown 语法支持

- ✅ 标题 (h1-h6)
- ✅ 粗体、斜体
- ✅ 列表（有序/无序）
- ✅ 代码块（自动语法高亮）
- ✅ 引用
- ✅ 链接
- ✅ 图片
- ✅ 表格
- ✅ 分隔线
